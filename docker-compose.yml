services:
  postgres:
    image: postgres:16
    container_name: biblioteca-postgres
    environment:
      POSTGRES_DB: biblioteca
      POSTGRES_USER: bibliotecas
      POSTGRES_PASSWORD: bibliotecas
    ports:
      - "5438:5438"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: